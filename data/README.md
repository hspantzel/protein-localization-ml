# Data

This directory contains data used for the protein localization project.

Raw and processed biological datasets are intentionally excluded from GitHub to avoid committing large files and to keep the project reproducible.

Planned structure:

```text
data/
├── raw/
└── processed/